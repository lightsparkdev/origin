export default function Home() {
  return (
    <main style={{ padding: '2rem' }}>
      <h1>Origin v2</h1>
      <p>Design system rebuild — Base UI + Figma-first approach.</p>
      <p>Start with <code>npm run storybook</code> to see components.</p>
    </main>
  );
}

